# Scripts

This directory is for lightweight helper scripts that wrap the implementation CLI or assist with local reproducible tasks.

Suggested uses:

- initializing a run
- validating local configuration
- preparing evaluation inputs
- exporting figure-ready tables
